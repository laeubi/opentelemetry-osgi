<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.opentelemetry.osgi</groupId>
        <artifactId>opentelemetry-osgi-parent</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>opentelemetry-osgi-agent</artifactId>
    <name>OpenTelemetry OSGi Agent Extension</name>
    <description>
        OpenTelemetry Java Agent extension that instruments OSGi framework internals
        using ByteBuddy bytecode instrumentation. Intercepts Framework.init(), Bundle
        lifecycle, BundleActivator, and BundleContext calls to produce traces, metrics,
        and logs. Vendor-agnostic: works with Felix, Equinox, or any OSGi R4+ framework.
    </description>

    <properties>
        <otel.javaagent.extension.version>2.15.0-alpha</otel.javaagent.extension.version>
        <bytebuddy.version>1.15.3</bytebuddy.version>
    </properties>

    <dependencies>
        <!--
            All dependencies are provided scope: the OTel javaagent supplies them at runtime.
            This module produces a plain extension JAR loaded via -Dotel.javaagent.extensions=
        -->

        <!-- OTel Java Agent Extension API (InstrumentationModule, TypeInstrumentation, matchers) -->
        <dependency>
            <groupId>io.opentelemetry.javaagent</groupId>
            <artifactId>opentelemetry-javaagent-extension-api</artifactId>
            <version>${otel.javaagent.extension.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- ByteBuddy for @Advice annotations -->
        <dependency>
            <groupId>net.bytebuddy</groupId>
            <artifactId>byte-buddy</artifactId>
            <version>${bytebuddy.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- OpenTelemetry API (GlobalOpenTelemetry, Tracer, Meter, Logger) -->
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-api</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- OSGi Framework API (compile-time references in advice code) -->
        <dependency>
            <groupId>org.osgi</groupId>
            <artifactId>org.osgi.framework</artifactId>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- This is NOT an OSGi bundle — skip bnd processing -->
            <plugin>
                <groupId>biz.aQute.bnd</groupId>
                <artifactId>bnd-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>generate-osgi-metadata</id>
                        <phase>none</phase>
                    </execution>
                </executions>
            </plugin>
            <!-- Use default manifest instead of bnd-generated one -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <configuration>
                    <archive combine.self="override">
                        <manifest>
                            <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
